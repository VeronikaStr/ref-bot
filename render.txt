services:
  - type: web
    name: ref-bot-for-money
    runtime: python
    region: frankfurt  # Или другой, ближайший к тебе
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn bot:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthcheck
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false  # Значение будет запрошено при деплое